name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: centos-latest
    
    steps:
    - uses: actions/checkout@v2
      name: change directory
      run: cd Server
    - name: compile
      run: chmod +x compile.sh
      run: ./compile.sh
