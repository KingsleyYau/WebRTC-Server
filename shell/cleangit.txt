git rev-list --objects --all | grep "$(git verify-pack -v .git/objects/pack/*.idx | sort -k 3 -n | tail -10 | awk '{print$1}')"

git filter-branch --force --prune-empty --index-filter 'git rm -rf --cached --ignore-unmatch common-c-library/third_party/ffmpeg/iOS/lib/libavcodec.a' --tag-name-filter cat -- --all

git push --force --all

rm -rf .git/refs/original/

git reflog expire --expire=now --all

git gc --prune=now